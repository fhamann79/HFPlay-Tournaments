@model IEnumerable<Domain.MatchTeamPlayerCard>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CardType.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.MatchTeamPlayer.MatchTeamPlayerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Minute)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CardType.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.MatchTeamPlayer.MatchTeamPlayerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Minute)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MatchTeamPlayerCardId }) |
            @Html.ActionLink("Details", "Details", new { id=item.MatchTeamPlayerCardId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MatchTeamPlayerCardId })
        </td>
    </tr>
}

</table>
